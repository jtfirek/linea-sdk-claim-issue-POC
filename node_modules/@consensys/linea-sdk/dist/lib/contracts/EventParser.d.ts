import { MessageInDb } from "../postman/utils/types";
import { Direction, MessageStatus } from "../postman/utils/enums";
import { TypedEventLog, TypedContractEvent } from "../../typechain/common";
import { ParsedEvent } from "../utils/types";
export declare class EventParser {
    static parseEvent<TEvent extends TypedEventLog<any>>(event: TEvent): ParsedEvent<TEvent>;
    static filterAndParseEvents<TEvent extends TypedEventLog<any>>(events: TEvent[]): ParsedEvent<TEvent>[];
    static parsedEventToMessage(event: ParsedEvent<TypedEventLog<TypedContractEvent>>, direction: Direction, status: MessageStatus): MessageInDb & {
        logIndex: number;
    };
}
