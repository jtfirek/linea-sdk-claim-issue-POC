import { EthersError, ErrorCode } from "ethers";
export type Mitigation = {
    shouldRetry: boolean;
    retryWithBlocking?: boolean;
    retryPeriodInMs?: number;
    retryNumOfTime?: number;
};
export type ParsedErrorResult = {
    errorCode: ErrorCode;
    context?: string;
    reason?: string;
    mitigation: Mitigation;
};
export declare class ErrorParser {
    static parseErrorWithMitigation(error: EthersError): ParsedErrorResult | null;
}
