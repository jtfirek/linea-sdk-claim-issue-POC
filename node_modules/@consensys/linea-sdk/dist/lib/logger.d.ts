import { LoggerOptions } from "winston";
export declare class LineaLogger {
    private logger;
    constructor(loggerName: string, options?: LoggerOptions);
    info(message: any): void;
    error(message: any): void;
    warn(message: any): void;
    debug(message: any): void;
    warnOrError(error: any, message: string): void;
}
export declare const getLogger: (loggerName: string, options?: LoggerOptions) => LineaLogger;
