"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isEmptyBytes = exports.subtractSeconds = exports.wait = void 0;
const wait = (timeout) => new Promise((resolve) => setTimeout(resolve, timeout));
exports.wait = wait;
const subtractSeconds = (date, seconds) => {
    const dateCopy = new Date(date);
    dateCopy.setSeconds(date.getSeconds() - seconds);
    return dateCopy;
};
exports.subtractSeconds = subtractSeconds;
function isEmptyBytes(bytes) {
    if (!bytes || bytes === "0x" || bytes === "" || bytes === "0") {
        return true;
    }
    const hexString = bytes.replace(/^0x/, "");
    return /^00*$/.test(hexString);
}
exports.isEmptyBytes = isEmptyBytes;
