import { DataSource } from "typeorm";
import { LoggerOptions } from "winston";
import { LineaLogger } from "../../logger";
import { MessageRepository } from "../repositories/MessageRepository";
import { DBCleanerConfig } from "../utils/types";
export declare class DatabaseCleaner {
    protected logger: LineaLogger;
    messageRepository: MessageRepository;
    protected enabled: boolean;
    protected shouldStopCleaning: boolean;
    protected cleaningInterval: number;
    protected msBeforeNowToDelete: number;
    constructor(dataSource: DataSource, config?: DBCleanerConfig, loggerOptions?: LoggerOptions);
    start(): Promise<void>;
    stop(): void;
    private databaseCleanerRoutine;
}
