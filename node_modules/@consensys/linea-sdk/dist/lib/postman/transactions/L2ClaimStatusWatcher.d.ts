import { DataSource } from "typeorm";
import { LoggerOptions } from "winston";
import { L2MessageServiceContract } from "../../contracts";
import { L2NetworkConfig } from "../utils/types";
import { ClaimStatusWatcher } from "./ClaimStatusWatcher";
export declare class L2ClaimStatusWatcher extends ClaimStatusWatcher<L2MessageServiceContract> {
    private readonly l2MessageServiceContract;
    constructor(dataSource: DataSource, l2MessageServiceContract: L2MessageServiceContract, config: L2NetworkConfig, loggerOptions?: LoggerOptions);
    isRateLimitExceededError(transactionHash: string): Promise<boolean>;
}
