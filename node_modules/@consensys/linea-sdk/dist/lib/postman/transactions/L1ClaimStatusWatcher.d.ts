import { DataSource } from "typeorm";
import { LoggerOptions } from "winston";
import { L1MessageServiceContract } from "../../contracts";
import { L1NetworkConfig } from "../utils/types";
import { ClaimStatusWatcher } from "./ClaimStatusWatcher";
export declare class L1ClaimStatusWatcher extends ClaimStatusWatcher<L1MessageServiceContract> {
    private readonly l1MessageServiceContract;
    constructor(dataSource: DataSource, l1MessageServiceContract: L1MessageServiceContract, config: L1NetworkConfig, loggerOptions?: LoggerOptions);
    isRateLimitExceededError(transactionHash: string): Promise<boolean>;
}
