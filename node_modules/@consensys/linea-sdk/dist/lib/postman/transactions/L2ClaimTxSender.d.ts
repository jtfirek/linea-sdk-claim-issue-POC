import { DataSource } from "typeorm";
import { LoggerOptions } from "winston";
import { L2MessageServiceContract } from "../../contracts";
import { L2NetworkConfig } from "../utils/types";
import { ClaimTxSender } from "./ClaimTxSender";
export declare class L2ClaimTxSender extends ClaimTxSender<L2MessageServiceContract> {
    constructor(dataSource: DataSource, l2MessageServiceContract: L2MessageServiceContract, config: L2NetworkConfig, originContractAddress: string, loggerOptions?: LoggerOptions);
    isRateLimitExceeded(_messageFee: string, _messageValue: string): Promise<boolean>;
}
