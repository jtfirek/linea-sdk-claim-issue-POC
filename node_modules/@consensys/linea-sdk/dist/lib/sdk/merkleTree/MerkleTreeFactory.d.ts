import { SparseMerkleTree } from "./SparseMerkleTree";
export declare class SparseMerkleTreeFactory {
    private readonly depth;
    constructor(depth: number);
    create(): SparseMerkleTree;
    createAndAddLeaves(leaves: string[]): SparseMerkleTree;
}
